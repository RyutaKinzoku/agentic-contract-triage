# Runtime dependencies for the agent service.
# Pinned to compatible ranges for reproducible builds; bump deliberately.
pydantic>=2.7,<3.0
pydantic-settings>=2.2,<3.0
fastapi>=0.110,<1.0
uvicorn[standard]>=0.29,<1.0